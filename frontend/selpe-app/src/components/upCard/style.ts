import { StyleSheet } from 'react-native';

export const upCardStyle = StyleSheet.create({
  container: {
    backgroundColor: '#FFEAA7',
    width: '100%',
    height: '100%',
    borderTopRightRadius: 50,
    borderTopLeftRadius: 50,
    alignItems: 'center',
    shadowColor: '#171717',
    shadowOffset: { width: 0, height: -4 },
    shadowOpacity: 0.2,
    shadowRadius: 3
  }
});
