import { StyleSheet } from 'react-native';

export const addStyle = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    width: 50,
    height: 50,
    padding: 12,
    borderRadius: 100,
    alignItems: 'center',
    justifyContent: 'center'
  }
});
