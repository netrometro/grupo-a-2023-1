import { StyleSheet } from 'react-native';

export const deleteStyle = StyleSheet.create({
  container: {
    backgroundColor: '#EB4D4B',
    padding: 12,
    borderRadius: 16
  },
  text: {
    color: '#fff'
  }
});
